# OTA development test keys

These Ed25519 keys are for local development OTA only.

- `dev_ed25519.seed.hex`: test signing seed used by host tests/local scripts.
- `dev_ed25519.pub.hex`: matching test public key.

Test key only. Never trusted by release firmware.
