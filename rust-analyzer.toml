# rust-analyzer configuration for mixed-target workspace
# Allows firmware to build for ESP32 while crates build for host

[cargo]
# Don't override individual package targets
# Let each package's .cargo/config.toml determine its target

[checkOnSave]
# Disable automatic checks to avoid target conflicts in mixed workspaces
enable = false
