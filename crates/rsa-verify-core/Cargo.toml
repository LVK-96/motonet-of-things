[package]
name = "rsa-verify-core"
version = "0.1.0"
edition = "2024"

[dependencies]
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
sha2 = { version = "0.10", default-features = true }
